import 'package:flutter/widgets.dart';

class WebShadowHost extends StatelessWidget {
  const WebShadowHost({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
