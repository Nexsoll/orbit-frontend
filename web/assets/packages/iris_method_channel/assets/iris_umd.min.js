/* Minimal Iris UMD stub to satisfy Flutter Agora Web worker import */
(function(){
  try { console.log('[Iris-Stub] iris_method_channel stub executing. self:', (typeof self)); } catch(e) {}
  var engine = {
    callApi: function(){ return Promise.resolve({result:0}); },
    initialize: function(){ return Promise.resolve(); },
    release: function(){ return Promise.resolve(); }
  };
  // Expose for worker/global
  self.createIrisApiEngine = function(){ return engine; };
  self.Iris = self.Iris || {};
  if (typeof self.Iris.createIrisApiEngine !== 'function') {
    self.Iris.createIrisApiEngine = self.createIrisApiEngine;
  }
  try { console.log('[Iris-Stub] iris_method_channel stub ready:', typeof self.createIrisApiEngine); } catch(e) {}
})();
