/* Minimal Iris UMD stub to satisfy Flutter Agora Web worker import (agora_rtc_engine path) */
(function(){
  var engine = {
    callApi: function(){ return Promise.resolve({result:0}); },
    initialize: function(){ return Promise.resolve(); },
    release: function(){ return Promise.resolve(); }
  };
  // Expose in worker/global scope
  self.createIrisApiEngine = function(){ return engine; };
  self.Iris = self.Iris || {};
  if (typeof self.Iris.createIrisApiEngine !== 'function') {
    self.Iris.createIrisApiEngine = self.createIrisApiEngine;
  }
})();
